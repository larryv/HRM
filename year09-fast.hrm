-- HUMAN RESOURCE MACHINE PROGRAM --

-- 9: Zero Preservation Initiative

-- INSTRUCTIONS: "Send only the ZEROs to the OUTBOX."

-- AVAILABLE COMMANDS: INBOX, OUTBOX, COPYFROM, COPYTO, ADD, JUMP, JUMPZ

-- SIZE: 6 commands (challenge 5)
-- AVERAGE RUNTIME: 21 steps (challenge 25)


-- The first item is always nonzero, and the second is always zero.

    INBOX
    INBOX
a:
    OUTBOX
b:
    INBOX
    JUMPZ    a
    JUMP     b


-- vim: set autoindent:
